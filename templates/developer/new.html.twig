{% extends 'layout/base.html.twig' %}

{% block main %}
    <h1>Create new Developer</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-success">Create Developer</button>
    {{ form_end(form) }}
{% endblock %}
